# VITE_BACKEND_URL intentionally left unset here for this verification run —
# axios then uses relative paths (same-origin, no CORS needed) and the Vite
# dev-server proxy in vite.config.ts falls back to its own default of
# localhost:8000, where the throwaway demo backend is listening.

VITE_FIREBASE_VAPID_KEY=BODAoU8ncITyTHrAVrTKm3-WiTPUnDd8lO_CPrqTVsktX1AXba_st_8Dc8hnuz6iAJWpiG-P173tbvTZoc4zb0w
